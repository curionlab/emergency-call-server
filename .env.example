# .env.example
# このファイルを .env にコピーして、各値を必ず自分用に設定してください。
# 実運用では決してこの例のまま使わないでください。

# サーバポート
PORT=3000

# 管理用ログインパスワード（/login 用）
# 例: 長くて推測しにくいランダム文字列を推奨
LOGIN_PASSWORD=change-this-admin-password

# JWT用シークレットキー（十分に長いランダム文字列を推奨）
JWT_SECRET=change-this-jwt-secret-to-random-string
REFRESH_TOKEN_SECRET=change-this-refresh-secret-to-random-string

# VAPID 鍵ペア
# 例:
#   npx web-push generate-vapid-keys --json
# で生成し、publicKey / privateKey をそれぞれ設定してください。
VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here

# VAPIDの連絡先メールアドレス
# mailto: 付きでも、メールアドレスだけでもOKです
VAPID_CONTACT_EMAIL=you@example.com

# フロントエンドのURL（CORS / 通知クリック先）
# 例: http://localhost:5173 や https://emergency-call-client.example.com など
CLIENT_URL=http://localhost:5173
